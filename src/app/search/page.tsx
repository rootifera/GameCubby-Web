export default function SearchPage() {
    return (
        <div style={{padding: 16}}>
            <h1 style={{fontSize: 24, margin: "0 0 12px 0"}}>Search</h1>
            <p style={{opacity: 0.8}}>
                This is a placeholder. We’ll add search inputs and results using <code>/search/basic</code> and{" "}
                <code>/search/advanced</code> endpoints next.
            </p>
        </div>
    );
}
