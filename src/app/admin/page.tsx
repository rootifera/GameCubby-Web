export default function AdminPage() {
    return (
        <div style={{padding: 16}}>
            <h1 style={{fontSize: 24, margin: "0 0 12px 0"}}>Admin</h1>
            <p style={{opacity: 0.8}}>
                This is a placeholder. We’ll add authentication and admin tools here
                (add/edit games, manage locations, sync IGDB data, etc.).
            </p>
        </div>
    );
}
